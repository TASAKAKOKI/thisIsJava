collect()메소드는 단순히 요소를 수집하는 기능 이외에 컬렉션의 요소들을 그룹핑해서 Map객체로 생성하는 기능도 제공한다.
	Collectros.groupingBy():의 리턴 객체를 매개값으로 대입
		스레드에 안전하지 않은 Map생성(싱글스레드환경에서 사용하는 컬렉션)
	Collectors.groupingByConcurrent():의 리턴 객체를 매개값으로 대입
		스레드에 안전한 ConcurrentMap생성(멀티스레드환경에서 사용하는 컬렉션)

		